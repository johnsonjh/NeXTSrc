OTHER_SRCS = Makefile
NOTES = RELEASE_NOTES-1 RELEASE_NOTES-3 RELEASE_NOTES-4 RELEASE_NOTES-5 \
	RELEASE_NOTES-6 RELEASE_NOTES-9 RELEASE_NOTES-10 RELEASE_NOTES-17 \
	RELEASE_NOTES-19 RELEASE_NOTES-20 RELEASE_NOTES-23 RELEASE_NOTES-24 \
	RELEASE_NOTES-25

NOTESDIR = /usr/local/doc/cc_proj/notes

install: ${DSTROOT}${NOTESDIR} ${NOTES}
	install -m 444 ${IFLAGS} ${NOTES} ${DSTROOT}${NOTESDIR}

${DSTROOT}${NOTESDIR}:; mkdirs $@

depend:

clean:

all:
